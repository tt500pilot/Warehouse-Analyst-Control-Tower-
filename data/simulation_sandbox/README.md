# Generated AWIA Simulation Data

Run:

```powershell
python .\scripts\generate_simulation_sandbox.py
```

This creates deterministic synthetic data under this directory.

The generated data is defined by `docs/architecture/simulation-sandbox.md` and seed `42`.

Do not use it as production data. Do not manually edit generated rows; change the generator and regenerate.
